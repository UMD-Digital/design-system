/**
 * @module Hero
 * @category Components
 * Hero components for prominent page headers and visual impact areas
 */

/**
 * Base hero component with multiple layout options
 * @see {@link base} for the base hero implementation
 */
export { default as base } from './base';

/**
 * Brand video hero component with animation support
 * @see {@link brandVideo} for the brand video hero implementation
 */
export { default as brandVideo } from './brand/video';

/**
 * Expandable hero component with sticky positioning
 * @see {@link expand} for the expandable hero implementation
 */
export { default as expand } from './expand';

/**
 * Logo-focused hero component
 * @see {@link logo} for the logo hero implementation
 */
export { default as logo } from './logo';

/**
 * Minimal hero component with theme variations
 * @see {@link minimal} for the minimal hero implementation
 */
export { default as minimal } from './minimal';
