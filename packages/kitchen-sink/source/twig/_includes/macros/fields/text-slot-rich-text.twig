{% macro RichText(params) %}
  {% set characters = params.characters ?? true %}
  {% set isStyled = params.isStyled ?? true %}
  {% set url = params.url ?? 'https://umd.edu' %}

  {% set string =
    'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qu.'
  %}

  {% if
    characters is defined and characters is not null and characters <= 400 %}
    {% set string = string|slice(0, characters) %}
  {% else %}
    {% set string = string|slice(0, 200) %}
  {% endif %}

  {% if isStyled == false %}
    <div slot="text">
      <p>
        {{ string }}
      </p>
    </div>
  {% endif %}

  {% if isStyled %}
    <div slot="text">
      <p>
        <strong>Duis reprehen</strong>
        <i>eu fugiat</i>.
        <a href="{{ url }}">
          <span class="sr-only">Text for Screen Reader</span>
          <span aria-hidden="true">Excepteur occaecat</span>
        </a>, voluptate. {{ string|slice(102) }}
        <em>Nostrud</em>
        <u>
          dolore nulla
        </u>.
      </p>
    </div>
  {% endif %}
{% endmacro %}

{% macro RichTextString(params) %}
  {% set characters = params.characters ?? true %}
  {% set isStyled = params.isStyled ?? true %}
  {% set url = params.url ?? 'https://umd.edu' %}

  {% set string =
    'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qu.'
  %}

  {% if
    characters is defined and characters is not null and characters <= 400 %}
    {% set string = string|slice(0, characters) %}
  {% else %}
    {% set string = string|slice(0, 200) %}
  {% endif %}

  {{ string }}
{% endmacro %}

{% macro CardRichTextSuccinct() %}
  {{
    _self.RichText({
      characters: 100,
      isStyled: true
    })
  }}
{% endmacro %}

{% macro CardRichTextNormal() %}
  {{
    _self.RichText({
      characters: 200,
      isStyled: true
    })
  }}
{% endmacro %}

{% macro CardRichTextVerbose(params) %}
  {{
    _self.RichText({
      characters: 400,
      isStyled: true
    })
  }}
{% endmacro %}

{% macro CardRichTextStringSuccinct() %}
  {{
    _self.RichTextString({
      characters: 100,
      isStyled: true
    })
  }}
{% endmacro %}

{% macro CardRichTextStringNormal() %}
  {{
    _self.RichTextString({
      characters: 200,
      isStyled: true
    })
  }}
{% endmacro %}

{% macro CardRichTextStringVerbose(params) %}
  {{
    _self.RichTextString({
      characters: 400,
      isStyled: true
    })
  }}
{% endmacro %}

{% macro OverlayRichTextSuccinct() %}
  {{
    _self.RichText({
      characters: 100,
      isStyled: false
    })
  }}
{% endmacro %}

{% macro OverlayRichTextNormal() %}
  {{
    _self.RichText({
      characters: 200,
      isStyled: false
    })
  }}
{% endmacro %}

{% macro OverlayRichTextVerbose(params) %}
  {{
    _self.RichText({
      characters: 400,
      isStyled: false
    })
  }}
{% endmacro %}
