{% extends '../_layout.twig' %}
{% import '../_elements/content-dropdown.twig' as Dropdown %}
{% import '../_ui/fields/headline.twig' as Headline %}
{% import '../_ui/fields/rich-text.twig' as RichText %}

{% block page %}
  <section>
    <div class="umd-lock">
      <h1 class="umd-sans-largest">
        Alert
      </h1>

      <h2 class="umd-sans-large">
        Content Variations
      </h2>

      {{ Dropdown.ContentVariations() }}

      <div id="content-succinct" aria-hidden="true">
        <section>
          <h3 class="umd-sans-large default">
            Default
          </h3>

          <umd-element-alert type="alert" days="0" icon="true">
            {{ Headline.HeadlineSuccinct() }}

            {{ RichText.RichTextSuccinct() }}

            <a href="https://google.com" slot="cta">Learn More</a>
          </umd-element-alert>
        </section>

        <section>
          <div class="umd-sans-large type-hr">
            <span class="type">Type</span>
            <span class="type-detail">Notification</span>
          </div>

          <umd-element-alert type="notification" days="0" icon="true">
            {{ Headline.HeadlineSuccinct() }}

            {{ RichText.RichTextSuccinct() }}

            <a href="https://google.com" slot="cta">Learn More</a>
          </umd-element-alert>
        </section>

        <section>
          <div class="umd-sans-large type-hr">
            <span class="type">Type</span>
            <span class="type-detail">Emergency</span>
          </div>

          <umd-element-alert type="emergency" days="0" icon="true">
            {{ Headline.HeadlineSuccinct() }}

            {{ RichText.RichTextSuccinct() }}

            <a href="https://google.com" slot="cta">Learn More</a>
          </umd-element-alert>
        </section>

        <section>
          <div class="umd-sans-large field-option-hr">
            <span class="field-option">Field Options</span>
            <span class="field-option-detail">Required Only</span>
          </div>

          <umd-element-alert type="alert" days="0" icon="true">
            {{ Headline.HeadlineSuccinct() }}
          </umd-element-alert>
        </section>
      </div>

      <div id="content-normal" aria-hidden="false">
        <section>
          <h3 class="umd-sans-large default">
            Default
          </h3>

          <umd-element-alert type="alert" days="0" icon="true">
            {{ Headline.HeadlineNormal() }}

            {{ RichText.RichTextNormal() }}

            <a href="https://google.com" slot="cta">Learn More</a>
          </umd-element-alert>
        </section>

        <section>
          <div class="umd-sans-large type-hr">
            <span class="type">Type</span>
            <span class="type-detail">Notification</span>
          </div>

          <umd-element-alert type="notification" days="0" icon="true">
            {{ Headline.HeadlineNormal() }}

            {{ RichText.RichTextNormal() }}

            <a href="https://google.com" slot="cta">Learn More</a>
          </umd-element-alert>
        </section>

        <section>
          <div class="umd-sans-large type-hr">
            <span class="type">Type</span>
            <span class="type-detail">Emergency</span>
          </div>

          <umd-element-alert type="emergency" days="0" icon="true">
            {{ Headline.HeadlineNormal() }}

            {{ RichText.RichTextNormal() }}

            <a href="https://google.com" slot="cta">Learn More</a>
          </umd-element-alert>
        </section>

        <section>
          <div class="umd-sans-large field-option-hr">
            <span class="field-option">Field Options</span>
            <span class="field-option-detail">Required Only</span>
          </div>

          <umd-element-alert type="alert" days="0" icon="true">
            {{ Headline.HeadlineNormal() }}
          </umd-element-alert>
        </section>
      </div>

      <div id="content-verbose" aria-hidden="true">
        <section>
          <h3 class="umd-sans-large default">
            Default
          </h3>

          <umd-element-alert type="alert" days="0" icon="true">
            {{ Headline.HeadlineVerbose() }}

            {{ RichText.RichTextVerbose() }}

            <a href="https://google.com" slot="cta">Learn More</a>
          </umd-element-alert>
        </section>

        <section>
          <div class="umd-sans-large type-hr">
            <span class="type">Type</span>
            <span class="type-detail">Notification</span>
          </div>

          <umd-element-alert type="notification" days="0" icon="true">
            {{ Headline.HeadlineVerbose() }}

            {{ RichText.RichTextVerbose() }}

            <a href="https://google.com" slot="cta">Learn More</a>
          </umd-element-alert>
        </section>

        <section>
          <div class="umd-sans-large type-hr">
            <span class="type">Type</span>
            <span class="type-detail">Emergency</span>
          </div>

          <umd-element-alert type="emergency" days="0" icon="true">
            {{ Headline.HeadlineVerbose() }}

            {{ RichText.RichTextVerbose() }}

            <a href="https://google.com" slot="cta">Learn More</a>
          </umd-element-alert>
        </section>

        <section>
          <div class="umd-sans-large field-option-hr">
            <span class="field-option">Field Options</span>
            <span class="field-option-detail">Required Only</span>
          </div>

          <umd-element-alert type="alert" days="0" icon="true">
            {{ Headline.HeadlineVerbose() }}
          </umd-element-alert>
        </section>
      </div>
    </div>
  </section>
{% endblock %}
