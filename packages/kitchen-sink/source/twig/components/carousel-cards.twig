{% extends '../_layout.twig' %}

{% import '../_elements/content-dropdown.twig' as Dropdown %}
{% import '../_ui/card-overlay.twig' as CardOverlay %}
{% import '../_ui/fields/headline.twig' as Headline %}
{% import '../_ui/fields/text-slot-rich-text.twig' as RichText %}
{% import '../_ui/fields/call-to-action.twig' as CallToAction %}

{% block page %}
  {% set imageOne = random(RecImageOptions) %}
  {% set imageTwo = random(RecImageOptions) %}
  {% set imageThree = random(RecImageOptions) %}
  {% set imageSquare = random(SquareImageOptions) %}
  {% set imageWeird = random(WeirdImageOptions) %}

  <section>
    <div class="umd-lock">
      <h1 class="umd-sans-largest">
        Carousel Cards
      </h1>

      <h2 class="umd-sans-large">
        Content Variations
      </h2>

      {{ Dropdown.ContentVariations() }}
    </div>
  </section>

  <div id="content-succinct" class="dropdown-selection" aria-hidden="true">
    <div class="umd-lock">
      <h3 class="umd-sans-large default">
        Default
      </h3>
    </div>
    <section>
      <umd-element-carousel-cards aria-hidden="true">
        {{ Headline.CarouselHeadlineSuccinct() }}

        {{ RichText.CardRichTextSuccinct() }}

        {{ CallToAction.CallToAction() }}

        <div slot="cards">
          {{
            CardOverlay.NormalCardBody({
              image: imageOne
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageTwo
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageThree
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageSquare
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}
        </div>
      </umd-element-carousel-cards>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large field-option-hr">
          <span class="field-option">Field Options</span>
          <span class="field-option-detail">Required Only</span>
        </div>
      </div>
      <umd-element-carousel-cards aria-hidden="true">
        {{ Headline.CarouselHeadlineSuccinct() }}

        <div slot="cards">
          {{
            CardOverlay.NormalCardBody({
              image: imageOne
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageTwo
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageThree
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageSquare
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}
        </div>
      </umd-element-carousel-cards>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large two-cards-hr">
          <span class="field-option">Edge Cases</span>
          <span class="two-card-detail">Two Cards</span>
        </div>
      </div>

      <umd-element-carousel-cards aria-hidden="true">
        {{ Headline.CarouselHeadlineSuccinct() }}

        {{ RichText.CardRichTextSuccinct() }}

        {{ CallToAction.CallToAction() }}

        <div slot="cards">
          {{
            CardOverlay.NormalCardBody({
              image: imageTwo
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageOne
            })
          }}
        </div>
      </umd-element-carousel-cards>
    </section>
  </div>

  <div id="content-normal" class="dropdown-selection" aria-hidden="false">
    <div class="umd-lock">
      <h3 class="umd-sans-large default">
        Default
      </h3>
    </div>
    <section>
      <umd-element-carousel-cards aria-hidden="false">
        {{ Headline.CarouselHeadlineNormal() }}

        {{ RichText.CardRichTextNormal() }}

        {{ CallToAction.CallToAction() }}

        <div slot="cards">
          {{
            CardOverlay.NormalCardBody({
              image: imageOne
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageTwo
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageThree
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageSquare
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}
        </div>
      </umd-element-carousel-cards>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large field-option-hr">
          <span class="field-option">Field Options</span>
          <span class="field-option-detail">Required Only</span>
        </div>
      </div>
      <umd-element-carousel-cards aria-hidden="false">
        {{ Headline.CarouselHeadlineNormal() }}

        <div slot="cards">
          {{
            CardOverlay.NormalCardBody({
              image: imageOne
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageTwo
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageThree
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageSquare
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}
        </div>
      </umd-element-carousel-cards>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large two-cards-hr">
          <span class="field-option">Edge Cases</span>
          <span class="two-card-detail">Two Cards</span>
        </div>
      </div>

      <umd-element-carousel-cards aria-hidden="false">
        {{ Headline.CarouselHeadlineNormal() }}

        {{ RichText.CardRichTextNormal() }}

        {{ CallToAction.CallToAction() }}

        <div slot="cards">
          {{
            CardOverlay.NormalCardBody({
              image: imageTwo
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageOne
            })
          }}
        </div>
      </umd-element-carousel-cards>
    </section>
  </div>

  <div id="content-verbose" class="dropdown-selection" aria-hidden="true">
    <div class="umd-lock">
      <h3 class="umd-sans-large default">
        Default
      </h3>
    </div>
    <section>
      <umd-element-carousel-cards aria-hidden="true">
        {{ Headline.CarouselHeadlineVerbose() }}

        {{ RichText.CardRichTextVerbose() }}

        {{ CallToAction.CallToAction() }}

        <div slot="cards">
          {{
            CardOverlay.NormalCardBody({
              image: imageOne
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageTwo
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageThree
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageSquare
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}
        </div>
      </umd-element-carousel-cards>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large field-option-hr">
          <span class="field-option">Field Options</span>
          <span class="field-option-detail">Required Only</span>
        </div>
      </div>
      <umd-element-carousel-cards aria-hidden="true">
        {{ Headline.CarouselHeadlineVerbose() }}

        <div slot="cards">
          {{
            CardOverlay.NormalCardBody({
              image: imageOne
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageTwo
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageThree
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageSquare
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              theme: 'dark'
            })
          }}
        </div>
      </umd-element-carousel-cards>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large two-cards-hr">
          <span class="field-option">Edge Cases</span>
          <span class="two-card-detail">Two Cards</span>
        </div>
      </div>

      <umd-element-carousel-cards aria-hidden="true">
        {{ Headline.CarouselHeadlineVerbose() }}

        {{ RichText.CardRichTextVerbose() }}

        {{ CallToAction.CallToAction() }}

        <div slot="cards">
          {{
            CardOverlay.NormalCardBody({
              image: imageTwo
            })
          }}

          {{
            CardOverlay.NormalCardBody({
              image: imageOne
            })
          }}
        </div>
      </umd-element-carousel-cards>
    </section>
  </div>
{% endblock %}
