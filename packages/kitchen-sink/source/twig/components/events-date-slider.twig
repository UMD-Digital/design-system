{% extends '../_layout.twig' %}

{% import '../_elements/content-dropdown.twig' as Dropdown %}
{% import '../_ui/date.twig' as DateDisplay %}
{% import '../_ui/fields/headline.twig' as Headline %}
{% import '../_ui/fields/link.twig' as Link %}

{% block page %}
  <section>
    <div class="umd-lock">
      <h1 class="umd-sans-largest">
        Events Date Slider
      </h1>

      <h2 class="umd-sans-large">
        Content Variations
      </h2>

      {{ Dropdown.ContentVariations() }}
    </div>
  </section>

  <div id="content-succinct" class="dropdown-selection" aria-hidden="false">
    <section>
      <div class="umd-lock">
        <h3 class="umd-sans-large default">
          Default
        </h3>
        <umd-element-events-date-slider theme="light" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleSuccinct() }}

          {{ Link.Link() }}

          <div slot="date-list">
            {{ DateDisplay.DateWithTitle() }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                link: true
              })
            }}
            {{ DateDisplay.DateWithSuccinctTitle() }}
            {{ DateDisplay.DateWithSuccinctTitle() }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                link: true
              })
            }}
            {{ DateDisplay.DateWithSuccinctTitle() }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large secondary-icon-options-hr">
          <span class="field-option">Field Options</span>
          <span class="field-option-detail">Without CTA</span>
        </div>

        <umd-element-events-date-slider theme="light" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleSuccinct() }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithSuccinctTitle({
                link: true
              })
            }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                link: true
              })
            }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large dark-hr">
          <span class="layout">Theme</span>
          <span class="layout-detail">Dark</span>
        </div>

        <umd-element-events-date-slider theme="dark" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleSuccinct() }}

          {{
            Link.Link({
              theme: 'dark'
            })
          }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithSuccinctTitle({
                theme: 'dark'
              })
            }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                theme: 'dark',
                link: true
              })
            }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                theme: 'dark',
                link: true
              })
            }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                theme: 'dark'
              })
            }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                theme: 'dark'
              })
            }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                theme: 'dark'
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large secondary-icon-options-hr">
          <span class="field-option">Edge Cases</span>
          <span class="three-dates-detail">Less than 3 dates</span>
        </div>

        <umd-element-events-date-slider theme="light" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleSuccinct() }}

          {{ Link.Link() }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithSuccinctTitle({
                link: true
              })
            }}
            {{
              DateDisplay.DateWithSuccinctTitle({
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>
  </div>

  <div id="content-normal" class="dropdown-selection" aria-hidden="false">
    <section>
      <div class="umd-lock">
        <h3 class="umd-sans-large default">
          Default
        </h3>

        <umd-element-events-date-slider theme="light" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleNormal() }}

          {{ Link.Link() }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithNormalTitle({
                link: true
              })
            }}
            {{ DateDisplay.DateWithNormalTitle({}) }}
            {{ DateDisplay.DateWithNormalTitle({}) }}
            {{
              DateDisplay.DateWithNormalTitle({
                link: true
              })
            }}
            {{ DateDisplay.DateWithNormalTitle({}) }}
            {{ DateDisplay.DateWithNormalTitle() }}
            {{ DateDisplay.DateWithNormalTitle({}) }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large secondary-icon-options-hr">
          <span class="field-option">Field Options</span>
          <span class="field-option-detail">Without CTA</span>
        </div>

        <umd-element-events-date-slider theme="light" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleNormal() }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithNormalTitle({
                link: true
              })
            }}
            {{
              DateDisplay.DateWithNormalTitle({
                link: true
              })
            }}
            {{
              DateDisplay.DateWithNormalTitle({
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large dark-hr">
          <span class="layout">Theme</span>
          <span class="layout-detail">Dark</span>
        </div>

        <umd-element-events-date-slider theme="dark" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleNormal() }}

          {{
            Link.Link({
              theme: 'dark'
            })
          }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithNormalTitle({
                theme: 'dark'
              })
            }}
            {{
              DateDisplay.DateWithNormalTitle({
                theme: 'dark',
                link: true
              })
            }}
            {{
              DateDisplay.DateWithNormalTitle({
                theme: 'dark',
                link: true
              })
            }}
            {{
              DateDisplay.DateWithNormalTitle({
                theme: 'dark'
              })
            }}
            {{
              DateDisplay.DateWithNormalTitle({
                theme: 'dark'
              })
            }}
            {{
              DateDisplay.DateWithNormalTitle({
                theme: 'dark',
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large secondary-icon-options-hr">
          <span class="field-option">Edge Cases</span>
          <span class="three-dates-detail">Less than 3 dates</span>
        </div>

        <umd-element-events-date-slider theme="light" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleNormal() }}

          {{ Link.Link() }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithNormalTitle({
                link: true
              })
            }}
            {{
              DateDisplay.DateWithNormalTitle({
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>
  </div>

  <div id="content-verbose" class="dropdown-selection" aria-hidden="false">
    <section>
      <div class="umd-lock">
        <h3 class="umd-sans-large default">
          Default
        </h3>

        <umd-element-events-date-slider theme="light" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleVerbose() }}

          {{ Link.Link() }}

          <div slot="date-list">
            {{ DateDisplay.DateWithVerboseTitle() }}
            {{
              DateDisplay.DateWithVerboseTitle({
                link: true
              })
            }}
            {{ DateDisplay.DateWithVerboseTitle() }}
            {{ DateDisplay.DateWithVerboseTitle() }}
            {{
              DateDisplay.DateWithVerboseTitle({
                link: true
              })
            }}
            {{ DateDisplay.DateWithVerboseTitle() }}
            {{
              DateDisplay.DateWithVerboseTitle({
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large secondary-icon-options-hr">
          <span class="field-option">Field Options</span>
          <span class="field-option-detail">Without CTA</span>
        </div>

        <umd-element-events-date-slider theme="light" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleVerbose() }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithVerboseTitle({
                link: true
              })
            }}
            {{
              DateDisplay.DateWithVerboseTitle({
                link: true
              })
            }}
            {{
              DateDisplay.DateWithVerboseTitle({
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large dark-hr">
          <span class="layout">Theme</span>
          <span class="layout-detail">Dark</span>
        </div>

        <umd-element-events-date-slider theme="dark" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleVerbose() }}

          {{
            Link.Link({
              theme: 'dark'
            })
          }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithVerboseTitle({
                theme: 'dark'
              })
            }}
            {{
              DateDisplay.DateWithVerboseTitle({
                theme: 'dark',
                link: true
              })
            }}
            {{
              DateDisplay.DateWithVerboseTitle({
                theme: 'dark',
                link: true
              })
            }}
            {{
              DateDisplay.DateWithVerboseTitle({
                theme: 'dark',
                link: true
              })
            }}
            {{
              DateDisplay.DateWithVerboseTitle({
                theme: 'dark',
                link: true
              })
            }}
            {{
              DateDisplay.DateWithVerboseTitle({
                theme: 'dark',
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>

    <section>
      <div class="umd-lock">
        <div class="umd-sans-large secondary-icon-options-hr">
          <span class="field-option">Edge Cases</span>
          <span class="three-dates-detail">Less than 3 dates</span>
        </div>

        <umd-element-events-date-slider theme="light" aria-hidden="true">
          {{ Headline.DateSliderComponentTitleVerbose() }}

          {{ Link.Link() }}

          <div slot="date-list">
            {{
              DateDisplay.DateWithVerboseTitle({
                link: true
              })
            }}
            {{
              DateDisplay.DateWithVerboseTitle({
                link: true
              })
            }}
          </div>
        </umd-element-events-date-slider>
      </div>
    </section>
  </div>
{% endblock %}
