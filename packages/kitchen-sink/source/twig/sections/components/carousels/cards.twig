{% extends '_layout.twig' %}

{% import '_includes/content-dropdown.twig' as Dropdown %}
{% import '_includes/macros/fields/call-to-action.twig' as CallToAction %}
{% import '../blocks/card-overlay.twig' as CardOverlay %}
{% import _self as macros %}

{% block page %}
  {{ macros.pagecontent() }}
{% endblock %}

{% macro pagecontent() %}
  <div>
    <section>
      <div class="umd-lock">
        <h1 class="page-heading">
          Carousel Cards
        </h1>

        <h2 class="content-heading">
          Content Variations
        </h2>

        {{ Dropdown.ContentVariations() }}
      </div>
    </section>

    <div id="normal" class="dropdown-selection" aria-hidden="false">
      <section>
        <div class="umd-lock">
          <h3 class="content-heading">
            Default
          </h3>
        </div>
        {{ macros.example() }}
      </section>

      <section>
        <div class="umd-lock">
          <div class="content-heading">
            <span>Field Options</span>
            <span>Required Only</span>
          </div>
        </div>
        <umd-element-carousel-cards resize>
          <h2 slot="headline">
            Headline Text
          </h2>

          <div slot="cards">
            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                theme: 'dark'
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                theme: 'dark'
              })
            }}
          </div>
        </umd-element-carousel-cards>
      </section>

      <section>
        <div class="umd-lock">
          <div class="content-heading">
            <span>Edge Cases</span>
            <span>Two Cards</span>
          </div>
        </div>

        <umd-element-carousel-cards resize>
          <h2 slot="headline">
            Headline Text
          </h2>

          <p slot="text" class="umd-rich-text">
            Rich Text
          </p>

          {{
            CallToAction.InternalCallToAction({
              theme: 'dark'
            })
          }}

          <div slot="cards">
            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}
          </div>
        </umd-element-carousel-cards>
      </section>
    </div>

    <div id="succinct" class="dropdown-selection" aria-hidden="true">
      <div class="umd-lock">
        <h3 class="content-heading">
          Default
        </h3>
      </div>
      <section>
        {{ macros.example() }}
      </section>

      <section>
        <div class="umd-lock">
          <div class="content-heading">
            <span>Field Options</span>
            <span>Required Only</span>
          </div>
        </div>
        <umd-element-carousel-cards resize>
          <h2 slot="headline">
            Headline Text
          </h2>

          <div slot="cards">
            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                theme: 'dark'
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                theme: 'dark'
              })
            }}
          </div>
        </umd-element-carousel-cards>
      </section>

      <section>
        <div class="umd-lock">
          <div class="content-heading">
            <span>Edge Cases</span>
            <span>Two Cards</span>
          </div>
        </div>

        <umd-element-carousel-cards resize>
          <h2 slot="headline">
            Headline Text
          </h2>

          <p slot="text" class="umd-rich-text">
            Rich Text
          </p>

          {{
            CallToAction.InternalCallToAction({
              theme: 'dark'
            })
          }}

          <div slot="cards">
            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}
          </div>
        </umd-element-carousel-cards>
      </section>
    </div>

    <div id="verbose" class="dropdown-selection" aria-hidden="true">
      <div class="umd-lock">
        <h3 class="content-heading">
          Default
        </h3>
      </div>
      <section>
        {{ macros.example() }}
      </section>

      <section>
        <div class="umd-lock">
          <div class="content-heading">
            <span>Field Options</span>
            <span>Required Only</span>
          </div>
        </div>
        <umd-element-carousel-cards resize>
          <h2 slot="headline">
            Headline Text
          </h2>

          <div slot="cards">
            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                theme: 'dark'
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                theme: 'dark'
              })
            }}
          </div>
        </umd-element-carousel-cards>
      </section>

      <section>
        <div class="umd-lock">
          <div class="content-heading">
            <span>Edge Cases</span>
            <span>Two Cards</span>
          </div>
        </div>

        <umd-element-carousel-cards resize>
          <h2 slot="headline">
            Headline Text
          </h2>

          {{ RichText.CardRichTextVerbose() }}

          {{
            CallToAction.InternalCallToAction({
              theme: 'dark'
            })
          }}

          <div slot="cards">
            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}

            {{
              CardOverlay.displayWithOptions({
                image: true
              })
            }}
          </div>
        </umd-element-carousel-cards>
      </section>
    </div>
  </div>
{% endmacro %}

{% macro example() %}
  <umd-element-carousel-cards resize>
    <h2 slot="headline">
      Headline Text
    </h2>

    <p slot="text" class="umd-rich-text">
      Rich Text
    </p>

    {{
      CallToAction.InternalCallToAction({
        theme: 'dark'
      })
    }}

    <div slot="cards">
      {{
        CardOverlay.displayWithOptions({
          image: true
        })
      }}

      {{
        CardOverlay.displayWithOptions({
          image: true
        })
      }}

      {{
        CardOverlay.displayWithOptions({
          theme: 'dark'
        })
      }}

      {{
        CardOverlay.displayWithOptions({
          image: true
        })
      }}

      {{
        CardOverlay.displayWithOptions({
          image: true
        })
      }}

      {{
        CardOverlay.displayWithOptions({
          theme: 'dark'
        })
      }}
    </div>
  </umd-element-carousel-cards>
{% endmacro %}
