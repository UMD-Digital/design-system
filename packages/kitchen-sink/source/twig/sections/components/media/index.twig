{% extends '_layout.twig' %}

{% import _self as macros %}

{% set MediaPages = [
  {
    name: 'Wrapping Text',
    link: 'wrapping-text'
  }
] %}

{% block page %}
  {{ macros.pagecontent() }}
{% endblock %}

{% macro GetChildUrl() %}
  {% import '../_data.twig' as Data %}

  {% set URL = Data.GetUrl() ~ 'media/' %}
  {{ URL }}
{% endmacro %}

{% macro SubLinks() %}
  {% for item in MediaPages %}
    <a href="{{ _self.GetChildUrl ~ item.link }}">{{ item.name }}</a>
  {% endfor %}
{% endmacro %}

{% macro pagecontent() %}
  <div style="padding-bottom: 50px;">
    <div class="umd-lock">
      <h1 class="page-heading">
        Media Elements
      </h1>

      <p>
        Additonal Component Examples:
      </p>

      <ul>
        {% for item in MediaPages %}
          <li>
            <umd-element-call-to-action type="secondary">
              <a href="{{ _self.GetChildUrl ~ item.link }}">{{ item.name }}</a>
            </umd-element-call-to-action>
          </li>
        {% endfor %}
      </ul>
    </div>

    <section>
      <div class="umd-lock">
        <div class="content-heading">
          <span>Wrapping Text</span>
          <a href="{{ _self.GetChildUrl() }}wrapping-text">
            View More Examples
          </a>
        </div>
      </div>
    </section>
  </div>
{% endmacro %}
