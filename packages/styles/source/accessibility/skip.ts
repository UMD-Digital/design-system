import { create } from '../utilities';

// umd-skip-content
export const content = create.jssObject({
  className: 'umd-skip-content',
  clip: 'rect(0,0,0,0)',
  borderWidth: '0px',
  height: '1px',
  margin: '-1px',
  overflow: 'hidden',
  padding: '0',
  position: 'absolute',
  whiteSpace: 'nowrap',
  width: '1px',
});
