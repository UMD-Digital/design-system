/**
 * @module element/action
 * Provides various action component styles including buttons, icons, and links.
 */

/**
 * Specialized button styles for multimedia and UI controls.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.action.button.fullScreen
 * ```
 * @since 1.1.0
 */
export * as button from './button';

/**
 * Icon-based action button styles.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.action.icon.small
 * ```
 * @since 1.1.0
 */
export * as icon from './icon';

/**
 * Outline button styles with various sizes and color schemes.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.action.outline.normal
 * ```
 * @since 1.1.0
 */
export * as outline from './outline';

/**
 * Primary button styles with emphasis for main actions.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.action.primary.normal
 * ```
 * @since 1.1.0
 */
export * as primary from './primary';

/**
 * Secondary button styles for less important actions.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.action.secondary.normal
 * ```
 * @since 1.1.0
 */
export * as secondary from './secondary';
