/**
 * @module element/asset
 * Provides styles for various media assets like images and GIFs.
 */

/**
 * GIF animation control styles.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.asset.gif.toggle
 * ```
 * @since 1.1.0
 */
export * as gif from './gif';

/**
 * Image display and layout styles with various aspect ratios and animation effects.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.asset.image.wrapper
 * Styles.element.asset.image.aspectSquare
 * ```
 * @since 1.1.0
 */
export * as image from './image';
