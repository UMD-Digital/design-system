/**
 * @module element/composite/card
 * Provides styles for various card components with different layouts and themes.
 */

/**
 * Block card component styles with different themes and layouts.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.composite.card.block.light
 * ```
 * @since 1.1.0
 */
export * as block from './block';

/**
 * List card component styles for vertical layouts with different themes.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.composite.card.list.light
 * ```
 * @since 1.1.0
 */
export * as list from './list';

/**
 * Overlay card component styles with color and image overlays.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.composite.card.overlay.color.light
 * ```
 * @since 1.1.0
 */
export * as overlay from './overlay';
