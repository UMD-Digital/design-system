/**
 * @module element/event
 * Provides styles for event-related components and displays.
 */

/**
 * Event metadata display components for dates, times, and locations.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.event.meta.container
 * ```
 * @since 1.8.0
 */
export * as meta from './meta';

/**
 * Event sign components for displaying event date and time indicators.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.element.event.sign.container
 * ```
 * @since 1.8.0
 */
export * as sign from './sign';
