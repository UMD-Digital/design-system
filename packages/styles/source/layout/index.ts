/**
 * @module layout
 * Layout components and styles for page structure.
 */

/**
 * Alignment utilities for positioning elements.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.layout.alignment
 * ```
 * @since 1.1.0
 */
export * as alignment from './alignment';

/**
 * Background styles and utilities.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.layout.background
 * ```
 * @since 1.1.0
 */
export * as background from './background';

/**
 * Grid layout system for responsive designs.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.layout.grid
 * ```
 * @since 1.1.0
 */
export * as grid from './grid';

/**
 * Spacing utilities for margins and padding.
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.layout.space
 * ```
 * @since 1.1.0
 */
export * as space from './space';
