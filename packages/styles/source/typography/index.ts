/**
 * @module typography
 * Typography styles and class use cases
 */

/**
 * @internal
 */

export * as fontFace from './font-face';

/**
 * Campaign fonts
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.campaign.fonts
 * ```
 * @since 1.8.0
 */

export * as campaign from './campaign';

/**
 * Elements fonts
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.elements.fonts
 * ```
 * @since 1.8.0
 */

export * as elements from './elements';

/**
 * Sans fonts
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.sans.fonts
 * ```
 * @since 1.8.0
 */

export * as sans from './sans';

/**
 * Serif fonts
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.serif.fonts
 * ```
 * @since 1.8.0
 */

export * as serif from './serif';

/**
 * Stat fonts
 * @example
 * ```typescript
 * import * as Styles from '@universityofmaryland/web-styles-library';
 * Styles.stats.fonts
 * ```
 * @since 1.8.0
 */

export * as stats from './stats';
