import { Layout } from '@universityofmaryland/variables';

export default {
  '.umd-lock-full': {
    ...Layout.lock.full,
  },

  '.umd-lock': {
    ...Layout.lock.max,
  },

  '.umd-lock-small': {
    ...Layout.lock.small,
  },

  '.umd-lock-smaller': {
    ...Layout.lock.normal,
  },

  '.umd-lock-extra-small': {
    ...Layout.lock.small,
  },

  '.umd-lock-smallest': {
    ...Layout.lock.smallest,
  },
};
